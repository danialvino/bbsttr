<div class="container">
    <h1>Você tem <%= pluralize(@bookings.count, 'agendamento', 'agendamentos') %> </h1>
  <% if @bookings.count == 0 %>
    Nenhum agendamento planejado.
  <% else %>
    <% @bookings.each do |booking| %>
      <%= booking %>
      <%= link_to "Avaliar a baby-sitter", new_booking_reviewssitter_path(booking) %>
    <% end %>
  <% end %>
</div>



