<div class="container">
  <% if @reviewsparents.count == 0 %>
    <h1>Você ainda não foi avaliada</h1>
  <% else %>
    <h1>Você recebeu <%= pluralize(@reviewsparents.count, 'avaliação', 'avaliações') %> </h1>

     <div class="synthesis">
      <h2>Nota geral</h2>

      <span style="display: block; width: 65px; height: 13px; background: url(/assets/star-rating-sprite.png) 0 0;"> <span style="display: block; width: <%= Reviewsparent.average(:rating).round(2)*20 %>%; height: 13px; background: url(/assets/star-rating-sprite.png) 0 -13px;"></span> </span>
      <p><%= Reviewsparent.average(:rating).round(2) %> / 5 (<%= pluralize(Reviewsparent.count, 'avalição', 'avaliações') %>)</p>
    </div>

    <h2>Detalhe</h2>


    <% @reviewsparents.each do |review| %>
    Data do baby-sitting : <%= review.booking.start_time %>
    <br>
    Endereço : <%= review.user.address %>
    <br>Pai que fez a avaliaçao : <%= review.user.name %>
    <br>
    Avaliaçao : <%= review.description %>
    <br>
    Nota recebida:
      <span style="display: block; width: 65px; height: 13px; background: url(/assets/star-rating-sprite.png) 0 0;"> <span style="display: block; width: <%= review.rating*20 %>%; height: 13px; background: url(/assets/star-rating-sprite.png) 0 -13px;"></span> </span>
    <br>
    ------ <br>
    <% end %>
  <% end %>
</div>
